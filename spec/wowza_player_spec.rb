require "spec_helper"

describe WowzaPlayer do
  it "has a version number" do
    expect(WowzaPlayer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
